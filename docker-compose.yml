version: "3.8"

services:
  openclaw:
    build: .
    ports:
      - "8080:8080"
    env_file: .env
    environment:
      PORT: "8080"
    volumes:
      - openclaw-data:/data
    restart: unless-stopped

volumes:
  openclaw-data:
